# macOS (Git only ignores untracked files — use `git rm -r --cached . && git add .`
# once if junk was committed before this file existed.)
**/.DS_Store
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / tooling
*.swp
*.swo
*~

# KiCad — local / machine-only (safe to ignore everywhere in this repo)
*.lck
**/*.lck
~*.lck
**/.history/
fp-info-cache
*-backups/
